#include "mainwindow.h"

#include <QApplication>

/*int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    MainWindow w(*new ACQUISITION_HANDLER(), *new STIMULI_HANDLER());
    w.show();
    return a.exec();
}
*/
