#include "linkedModuleBuilder.h"



map<linkedModuleName, SIM_NODE*> LinkedModuleBuilder::buildLinkedModule(objectBuilderOutput* objBuilderOutput)
{

    map<linkedModuleName, SIM_NODE*> builtLinkedModules;

    // for(objectBuilderOutput::iterator it = objBuilderOutput->begin(); it != objBuilderOutput->end(); it++)
    // {
       
    // }

    return *(new map<linkedModuleName, SIM_NODE*>());
}




